import Route from './Route';
export default class Redirect extends Route {
    constructor(props, context) {
        super(props, context);
        if (!props.to) {
            props.to = '/';
        }
    }
}
